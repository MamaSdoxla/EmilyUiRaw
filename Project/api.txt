-- Создание UI
Library.createSection(parent, text)
Library.createLabel(parent, text)
Library.createContentButton(parent, text, callback, customColor)
Library.createTextBox(parent, placeholder, font)
Library.createSlider(parent, labelText, min, max, getval, onval, fmt)
Library.createDropdown(parent, labelText, getOptions, getCurrent, onselect)
Library.createColorInput(parent, labelText, getcol, oncol)
Library.createToggle(parent, labelText, initial, callback)

-- Регистрация
Library.registerKeyListProvider(group, fn)
Library.registerToggle(btn, getState)
Library.registerThemeUpdater(fn)
Library.onUnlock(fn)

-- Утилиты
Library.notify(title, text)
Library.copyDiscord()
Library.makeDraggable(dragFrame, targetFrame)
Library.getFOVCircle()
Library.scaleColor(c, f)
Library.lighter(c, amt)
Library.darker(c, amt)
Library.parseRGB(str)
Library.formatColor(c)

-- Состояние
Library.unlocked
Library.currentToggleKey
Library.uiColor_*
Library.FuckYou, Library.ScreenGui, Library.Containment, Library.MenuInsided
Library.tabs, Library.moduleToggles, Library.themeElements